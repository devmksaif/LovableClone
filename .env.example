# AI Provider Configuration (choose one or more)
# Priority: Groq (fastest) → Gemini (reliable) → OpenRouter (fallback)

# Groq API (Fastest - Recommended)
# Get your API key from: https://console.groq.com/keys
GROQ_API_KEY=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
# Optional: Groq model to use (default: llama3-8b-8192)
GROQ_MODEL=llama3-8b-8192

# Alternative: Google Gemini API (High Quality)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
# Optional: Gemini model to use (default: gemini-1.5-pro)
GEMINI_MODEL=gemini-1.5-pro

# Fallback: OpenRouter API
# Replace the value with your actual OpenRouter API key
OPENROUTER_API_KEY=your-openrouter-key-here
# Optional: override the default OpenRouter base
OPENROUTER_API_BASE=https://openrouter.ai/api/v1
# Optional: model to use via OpenRouter (example)
OPENROUTER_MODEL=minimax/minimax-m2:free
# Optional: temperature
OPENAI_TEMPERATURE=0.7

# MongoDB connection string for Prisma
# Replace with your MongoDB connection string (local or MongoDB Atlas)
# Local: mongodb://localhost:27017/nextlovable
# Atlas: mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/nextlovable
DATABASE_URL="mongodb://localhost:27017/nextlovable"
